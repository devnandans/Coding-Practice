
# Q 5  https://practice.geeksforgeeks.org/problems/multiply-left-and-right-array-sum1555/1
#Multiply left and right array sum.



// { Driver Code Starts
//Initial Template for C++

#include <bits/stdc++.h>
using namespace std;

int multiply(int arr[], int n);


int main() {
	//code
	int t;
	cin>>t;
	while (t--)
	{
	    int n,sum1=0,sum2=0;
	    cin>>n;
	    int a[n];
	    for(int i=0;i<n;i++)
	        cin>>a[i];
	    
	    cout << multiply(a, n) << endl;
	    
	}
	return 0;
}// } Driver Code Ends


//User function Template for C++

int multiply(int arr[], int n)
{
    int s1=0,s=0,sol;
    
    for(int i=0;i<n/2;i++)
    {
        
        s1=s1+arr[i];
        
    }
    
    
    for(int j=n/2;j<n;j++)
    {
        s=s+arr[j];
        
        
    }
    
    sol = s*s1;
    return sol;
}